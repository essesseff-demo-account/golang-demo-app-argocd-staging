apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: golang-demo-app-staging
  namespace: argocd
  labels:
    app: golang-demo-app
    environment: staging
    essesseff: "true"
    configenvrepoid: "1199083862"
spec:
  project: default
  source:
    repoURL: https://github.com/essesseff-demo-account/golang-demo-app-config-staging
    targetRevision: main
    path: .
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: essesseff-demo-account
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
  info:
    - name: golang-demo-app
      value: "golang-demo-app GitOps deployment managed by essesseff"
    - name: environment
      value: "staging"
      
